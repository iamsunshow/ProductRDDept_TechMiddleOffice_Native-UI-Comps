# 回归测试台账（错误防复发清单=唯一权威）

> 依据：《产研部-唯一权威手册》第二章第四节「错误反馈回归闭环」+ 本仓 AGENTS.md 红线「修复必带回归收口」。
> 定位：本台账=所有历史/新增错误的全量登记与「问题→防复发落点」映射=C1 单测补建与回归补测的逐批需求清单。
> 建档：2026-09-07（以子仓 git 历史 fix 提交为准，全量从旧到新整理）。后续每发现/修复一条错误=必须当场在本台账补行=不补=视为未完成。
>
> 防复发落点分四类：
> - L1=Android Robolectric 单测（`cd android && ./gradlew :components:testDebugUnitTest` 真绿才入库；测试文件按组件落在 android/components/src/test）
> - L2=编译门禁（安卓 gradlew assembleDebug 退出码0 / iOS 用户 Xcode 构建零报错）+ 第四章永久禁令
> - L3=双端 Demo 场景一致性门禁（scripts/check_demo_parity.py）
> - L4=人工实机回归清单（iOS 渲染/手势/动效类，自动化不可达=依赖 demo 实机复验）
>
> 全量 fix 提交原始清单生成命令：`git log --oneline --grep="fix"`（本台账按组件/主题聚合建档，逐条明细以此命令复核）。

## 第一节 2026-09 组件流 C1.5 用户实机/复验反馈错误明细（最活跃批=优先补测）

| # | 组件 | 现象（用户视角） | 根因 | 修复提交 | 防复发落点 | 回归状态 |
|---|------|------------------|------|----------|------------|----------|
| 1 | Divider | iOS Demo4 分割线不均分 | Row leading/trailing 约束+UIStackView fill 挤最左 | 5456e38 | L4+L2 | 待补测 |
| 2 | Divider | iOS Demo4 说明文字与行重叠 | 段容器删 bottom 致高度链断裂溢出 | 5456e38 | L4 | 待补测 |
| 3 | Divider | Android Demo3 带文本截断 | 固定高 16 裁 Text 行框 | 5456e38 | L1 | 待补测 |
| 4 | Layout | iOS 行右缘外边距小于 Android | 宽度分配未扣 gutter 行总宽溢出 | dadaf09 | L1+L4 | 待补测 |
| 5 | Layout | iOS Demo3 药丸文字偏左 | label 被拉宽默认左对齐 | ae39dbb | L4 | 待补测 |
| 6 | SafeArea | C1.5 首验 D1/D3/D4 Android 中部异常避让 | insets 消费链未按边避让 | 962010a→5b374a8 | L1+L4 | 待补测 |
| 7 | SafeArea | Android 容器被压成灰条 | 错用 windowInsetsTopHeight（元素尺寸非 padding） | 5b374a8 | L2+L1 | 待补测 |
| 8 | Space | iOS Demo1-3 首个元素被拉伸 | UIStackView .fill 把富余摊给最低 hugging 子项 | 680e852→5651ff4 | L4 | 待补测 |
| 9 | Space | iOS 编译 required init(coder:) 'nil' 语法 | 无上下文类型推断 | 4d376d5 | L2 | 待补测 |
| 10 | Sticky | iOS D1-D4 拖动不悬停 | contentSize=0 滚动域缺失+顶替候选振荡 | f220dad | L4 | 待补测 |
| 11 | BackTop | iOS D2 按钮不出现 | setFace removeFromSuperview 拔除承载按钮 | e641e2f | L2+L4 | 待补测 |
| 12 | BackTop | iOS D3 计数反馈不可见 | 反馈在页面顶部共享条非段内 | f5f49d9 | L3 | 待补测 |
| 13 | FixedNav | Android 面板通栏/图标角标错位/数字截断 5 类 | 缺 IntrinsicSize/行尾无弹性/徽标固定高 | 9503a47 | L1+L4 | 待补测 |
| 14 | FixedNav | iOS D3/D4 钮可见却点不动 | 组件无 intrinsic 尺寸+父 bounds 不含钮+hittest 不下钻 | 9caf671→4634bbb | L4 | 待补测 |
| 15 | FixedNav | iOS 多次点击后失灵 | 旧动画 completion 无条件清引用误清新卡 | 9503a47 | L4 | 待补测 |
| 16 | SideBar | Android demo 入口不可点 | 注册行 demo=null | c11d92a | L2 | 待补测 |
| 17 | HoverButton | icon-only ✚ 不水平居中 | Row 撑满+Arrangement.Start | c11d92a | L1 | 待补测 |
| 18 | SideBar | iOS D2/D4 受控不回写高亮 | 半受控 demo 缺回写链 | 97c4504 | L4 | 待补测 |
| 19 | NavBar | iOS demo 说明文字与导航条重叠 | 顶部单锚缺 bottom 闭合 | 49f1bef | L4 | 待补测 |
| 20 | Tabbar | Android badge 不在图标右上且数字不居中 | offset y 作用于角标上缘+Text 行高空隙 | 2cabefa | L1 | 待补测 |
| 21 | Tabbar/Tabs | iOS D1-D4 点击不切换高亮 | 半受控 onChange 不回写组件 selectedValue | 18545fc | L4 | 待补测 |
| 22 | Form/InputNumber | iOS 编译 6 error | attributedText 可选解包+init 形参遮蔽 min/max | 9ac1fef | L2 | 待补测 |
| 23 | Checkbox/Input | iOS Showcase 段构建崩溃 | pinFullWidth 先建约束未 addSubview | 77d56ad | L2+L4 | 待补测 |
| 24 | Address/Cascader | iOS init 崩溃 | chipsBar 未入树即建 hairline 约束 | e8ee260 | L2+L4 | 待补测 |
| 25 | Rate | iOS 星区整条黑色背景 | 纯 draw 未关 isOpaque+未设 clear 背景 | fe78956 | L2+L4 | 待补测 |
| 26 | NumberKeyboard | iOS 键盘不可点+文字错位 | demo 段容器高度链断裂 键盘溢出父 bounds | db9d2c5 | L4 | 待补测 |
| 27 | NumberKeyboard | iOS 数字 1~9 键点击零事件 | makeKey 未传 action（有 action 才 addAction） | 5d371b9 | L4 | 待补测 |
| 28 | Picker | Android 松手吸附中心行判错 | 中心行判定法受 contentPadding 偏移 | 884d479 | L1 | 待补测 |
| 29 | Picker | Android 慢拖停在两行中间 | fling 吸附兜底缺失 | ae9908c | L1 | 待补测 |
| 30 | Range | Android demo 无法操作 | 手势串行陷阱+key 含受控 value | 0e6bbab | L1 | 待补测 |
| 31 | Switch | iOS demo 编译 unterminated string literal | 字符串插值内嵌转义引号 | （Switch 门禁 B 批） | L2 | 待补测 |
| 32 | TextArea | iOS Demo pbxproj 未登记 | Xcode 工程手工登记遗漏 | 98d2646 | L2 | 已建成 TextAreaTest 首批 |
| 33 | TextArea | iOS Demo AppColor.danger 不存在 | token 名用错 | 2008007 | L2 | 待补测 |
| 34 | Uploader | iOS 访问级别编译错 | fileprivate 过度 | e4dfd84 | L2 | 待补测 |
| 35 | Uploader | iOS maskView 与 UIView 冲突 | 命名撞系统属性 | e84fa09 | L2（第四章已钉） | 待补测 |
| 36 | Uploader | iOS UploadItem memberwise init 异常/非必填无默认 | 结构体 init 行为异常 | dba8960→f4778e0 | L2 | 待补测 |
| 37 | Uploader | Android LazyVerticalGrid 嵌套 crash | 网格组件套滚动容器 | 69910e5 | L1（布局结构用例） | 已建成 UploaderTest 首批 |
| 38 | Uploader | Android 删除角标 × 偏离中心 | 组合角标内对齐算法 | e264918 | L1 | 待补测 |
| 39 | Uploader | Android 网格间距不均分 | Row 外层留间距 | ef6131a | L1 | 待补测 |
| 46 | Dialog | iOS D1-D4 按钮点击+遮罩点击全无响应（弹窗可见但事件失联） | DialogViewController.show() 仅挂载 view 到 keyWindow，自身无强引用持有者；调用方局部变量释放→VC 立即回收；UIButton.addTarget/UIGestureRecognizer target 持弱引用→target 变 nil→点击零事件 | （本次提交） | L2+L4 | 待补测 |
| 47 | Popup | Android 蒙版透明度明显高于 iOS | Android 用 Dialog 会添加系统级 dim 层（~0.6 alpha）叠加在 0.45 黑色蒙版上，总暗度远超 iOS | f45ea96 系列 | L1（PopupTest 蒙版存在+点击语义）+L4（alpha 实机比对） | 已建成 PopupTest |
| 48 | Popup | center 气泡尺寸过小（几乎看不到文字） | center 容器无 minWidth 约束，内容短时弹层过小 | f45ea96 系列 | L1（PopupTest center minWidth≥240dp） | 已建成 PopupTest |
| 49 | Popup | bottom 弹层被导航栏遮挡高度不足（Android 1.5 按钮 vs iOS 3 按钮） | v1.4.11 去掉 navigationBarsPadding 后弹层贴到屏幕底部被导航栏遮挡；v1.4.12 修复=navigationBarsPadding 放 heightIn 之前，heightIn 限制 content 高度不含 padding | f45ea96 | L1（PopupTest bottom minHeight≥120dp） | 已建成 PopupTest |
| 50 | Popup | 双端 padding 不一致导致弹层总高度差 12dp | iOS content 约束 top=closeable?40:4 + start/end=16 + bottom=4；Android 旧版 center 容器 horizontal=16 + content top=closeable?32:0 + 无 bottom | 5258311 | L1（PopupTest closeable 高度≥40dp / 非 closeable 对比） | 已建成 PopupTest |
| 51 | Popup | Demo1/3/4 Text fillMaxWidth 导致弹层通栏 | Popup 用 androidx.compose.ui.window.Popup 全屏约束，fillMaxWidth 让 Text 占满全屏宽度→容器 defaultMinSize 取 max(全屏,240dp)=全屏 | f45ea96 | L1（PopupTest center minWidth 反向校验=不通栏）+L3（Demo 一致性脚本） | 已建成 PopupTest |
| 52 | Popup | closeable 关闭按钮 | 关闭按钮 24dp 圆形灰底白叉 | — | L1（PopupTest closeable 按钮存在+24dp / 非 closeable 不存在） | 已建成 PopupTest |
| 53 | PickerView | Android Demo 整页无法拖动、D3/D4 够不到 | 宿主详情页 TmoDemo 为不带滚动的 Column，PickerViewDemo 堆叠 4 个完整 Picker（各 264dp）总高远超一屏且自身无 verticalScroll（同类历史问题见 MainActivity:1100 注释） | （v1.5.2 本次提交） | L4（实机拖页+滚轮双手势）+禁令：Demo 页内容超一屏必须自带 verticalScroll，内层定高滚轮可与外层同滚（PickerDemo 已验收） | 待补测 |
| 54 | Slider | Android thumb 圆形在横线上方、未与轨道垂直居中 | 轨道/激活段 CenterStart（44dp 容器内已垂直居中）后又叠加 offset(y=(44-4)/2=20dp)，轨道二次下移到 y=40 贴底（中心42），thumb 中心22 → 圆在横线上方 20dp | （v1.5.4 本次提交） | L1（SliderTest 7 例真绿：0/50/100 三档三层中心Y=容器中心+尺寸同轴+点击两端+分档吸附）+禁令：Box 内 Alignment 已负责该轴定位，禁止再用 offset 做同轴二次定位 | 已建成 SliderTest |
| 55 | Steps | 双端横向连线与节点断开（空半列）；Android 竖向另有 16dp 断口 | 设计规格 .line{left:50%;right:-50%}=圆心→圆心贯穿；两端旧实现只画到等宽 cell 边界（Android 仅右半段、iOS leading=圆右缘/trailing=容器右缘）；Android 竖向行距 16dp 放在连线之外的行外 Spacer | （v1.5.6 本次提交，原拟 1.5.5 被 ecb4285 并发占用顺延） | L1（StepsTest 3 例真绿：横向 4 圆+左右半段各3+三连线圆心→圆心无缝拼接垂直居中、竖向圆底→下一圆顶零间隙）+禁令：等分 cell 步骤条连线必须圆心到圆心、圆片层级在线之上；行距必须并入连线高度，禁止线外挂 Spacer | 已建成 StepsTest |
| 56 | Tag | Android D2/D4 文字（及 D4 关闭叉）未垂直居中 | Text 默认行高（≈字号×1.2+）大于字号，行盒虽被 Row CenterVertically 居中，字形仍按 baseline 落位致视觉中线偏移；Close 矢量图标严格几何居中→两者错位；D1/D2 外层 Row 还漏写 verticalAlignment | （v1.5.9 本次提交，1.5.8 被 d420032 Slider 并发占用顺延） | L1（TagTest 4 例真绿：三尺寸文字中心Y=标签中心+高度 20/24/28dp、文字与叉同轴+间隙 2dp+叉 12dp、点叉回调、非 closeable 无叉）+禁令：固定小高度胶囊内文字必须 lineHeight=fontSize 配 LineHeightStyle(Center,Trim.Both)；横排 Demo 外层 Row 必须显式 verticalAlignment | 已建成 TagTest；字形像素级视觉居中留 L4 实机复验 |
| 57 | VirtualList（+Card/ImageView 同批收口） | Android Demo 页无法拖动、D2-D4 不可达（模式性故障第 5 次） | 宿主详情页 TmoDemo 为不带滚动的 Column（#53 同病），VirtualListDemo 自带 fillMaxSize Column 但无 verticalScroll 被约束一屏高，四个列表块 1300dp 溢出不可达；历史同病=Empty Demo/Drag Demo/#53 PickerView | （v1.6.2 本次提交，1.6.1 被 7d49a65 DropDown 并发占用顺延；组件层零改动） | 脚本扫描全部 75 个 Demo 页面级容器一次收口：VirtualListDemo/CardDemo 补 verticalScroll（内层定高 LazyColumn 与外层 nested scroll 自动衔接）、ImageViewDemo 顶层散排包可滚 Column；iOS=UIScrollView 宿主+UITableView 定高可滚不同病。禁令升级：Demo 页内容超一屏必须自带 verticalScroll（页面级 LazyColumn 亦可），新增 Demo 一律过此门禁 | L4 实机复验：页面可拖、D1-D4 全可达、列表滚到边界续滚页面 |
| 58 | LineChart | Android 纵坐标项与 X 轴首项挤在一起；数据节点实心小圆（预期空心圆） | ① 坐标域不同：iOS DGCharts axisMinimum=-0.5/Maximum=count-0.5 首末点内缩半步长，Android 从左缘起画 i*stepX，首标签左半字伸入 Y 轴列；② 单位错误：Android 圆 r5f/白点 2f 裸 px（≈1.9/0.76dp）线宽 4px，iOS 基准 circleRadius 3pt+hole 1.5pt/.white+lineWidth 2pt，Android 又小又实视觉即实心圆 | （v1.6.5 本次提交，iOS 为基准零改动） | x 映射三处统一 xFor()=(i+0.5)/count 内缩域；圆点 dp 化=彩圆 3dp+白洞 1.5dp 白色、线宽 2dp。禁令：图表类 Canvas 数据点/圆/线尺寸一律 dp→px 禁止裸 px；坐标域必须与 iOS 同构（含 -0.5 内缩） | :components 编译 0 错误；:app 编译被并发会话 DemoPage 重构半成品阻塞，待复验；L4 实机：首标签不顶 Y 轴、节点空心圆 |
| 59 | LineChart | Android 数据紧贴图表边缘、网格横线不可见（iOS 有内边距+横线可见）；另澄清双端 Demo3 数据一致 | ① 内边距缺失：iOS chartView 有 edges.inset(top/bottom=sm,left/right=md)，Android Box 满铺无 padding；② 网格线不可见：strokeWidth=0.5f 裸 px（真机≈0.19dp 亚像素），iOS 默认 0.5pt——#58「禁止裸 px」禁令再次命中；③ y 域顶格：iOS DGCharts 默认 spaceTop/spaceBottom=0.1，Android [0,max] 顶格。用户所报 iOS Demo3 红绿双线与源码不符（iOS Demo3=仅收入单绿线，双线在 Demo1，双端四段数据逐值一致），系段落编号误记 | v1.7.5 | 根 Box 补 padding(sm/md) 对齐 iOS inset；网格线宽 0.5.dp→px；新增 yFor() 对齐 spaceTop/Bottom=0.1（网格/Y 标签/折线/圆点四处统一映射） | :components 编译 0 错误（20s）；L4 实机复验：图表四周有留白、横网格线可见、数据不贴上下缘——**用户实机验收通过（v1.7.5，原话「LineChart：已通过」），双端 Demo passed=true 已同步** |
| 64 | DropDown | 六项视觉/交互双端不一致（用户 2026-09-12）：①iOS 菜单出现在标题下（Android 在选中值文字下）②iOS 选项有横线③Android D1 选中仅绿字无对勾（iOS 绿字+✓）④D2 按钮 iOS 显白 Android 显灰⑤Android D4 点选无反应⑥iOS D4 值文字顺序排列（Android 居右） | ①iOS 锚点=triggerButton 整行；②UITableView 默认 singleLine 分隔；③Android D1 DropdownMenuItem 只有绿字；④bgPage=F9FAFB 白卡上 ΔE≈1% 肉眼不可见，非组件 bug 是 token 过浅；⑤Android 纯受控无内部态、D4 Demo 未传 onValueChange；⑥title/value 两标签横向 hugging 同优先级(250)竞争，值标签可停在标题旁 | v1.8.0（原拟 v1.7.9，被并发会话 ImagePreview 9ecb1b1 占用顺延） | ①单列锚点改右侧「值文字+箭头」计算 rect（右缘超屏左移）；②separatorStyle=.none+selectionStyle=.none；③Android D1 补 ✓（双端=绿字+对勾）；④双端 D2 按钮统一 black alpha 0.04；⑤Android innerValue=remember(value) 内部选中态（对齐 iOS valueStorage）；⑥titleLabel hugging/compression required+valueLabel leading 改 ≥。禁令：半受控组件必须有内部态兜底（无回调也要响应）；同行 Text 左右分布时左件必须 hugging required | :components:compileDebugKotlin 0 错误（13s）+iOS xcodebuild BUILD SUCCEEDED；L4 实机：iOS 面板从值文字下弹出、无横线、选中绿字+✓、D2 双端灰底一致、Android D4 点选即时更新、双端 D4 值居右 |
| 62 | DropDown | iOS 五项交互问题（用户 2026-09-12）：①Demo1 面板错位「跑到前面」②双端文字-箭头间距时大时小③iOS D2 面板文字截断④iOS 点空白不关闭⑤Android D2 撑开内容区域（应悬空） | ①openPanel window 为 nil 时面板挂 self（44pt 触发行内）→错位；②Android width(120.dp) 固定宽/iOS 8pt+D2 箭头钉按钮 trailing 间隙随内容变化；③v1.7.7 把 D2 面板宽也改内容自适应（≈81pt<列宽）截断；④iOS 无面板外点击手势；⑤Android D2 手写 Column 面板在文档流内撑开 | v1.7.8 | ①面板只挂 window+bringSubviewToFront（未上屏不弹）；②双端固定 4 间距：Android widthIn(max)+spacedBy(4)、iOS 8→4、D2 箭头紧跟 title；③新增 PanelWidthMode：D2=matchAnchor（列宽）、单列=contentAdaptive；④window 异步 tap 手势+cancelsTouchesInView=false+delegate 过滤面板内触摸；⑤Android D2 改 Material3 DropdownMenu 悬浮层 | :components:compileDebugKotlin 0 错误（18s）+iOS xcodebuild BUILD SUCCEEDED；L4 实机：iOS D1 面板在触发文字正下方、双端文字-箭头 4 间距恒定、iOS D2 面板=列宽不截断、iOS 点空白关闭、Android D2 悬空不撑开 |
| 61 | Tour | iOS Demo1 点击下一步无反应（只能跳过）；Demo2 无法关闭；Demo1-4 按钮文字紧贴边缘 | ① Demo1-4 均缺 `tour.onChange = { tour.current = $0 }` 回调，`nextTapped()` 调 `onChange?(current+1)` 但 current 不更新、`updateContent()` 不触发，步骤指示/按钮文字停滞；Demo2 `showSkip=false` 只能走"下一步→完成"路径，但 isLast 永远 false → "完成"按钮永不出现 → 引导层无法关闭；② `nextButton`/`prevButton`/`skipButton` 三按钮均未设 `contentEdgeInsets`，文字紧贴按钮边缘，Android `TourButton` 用 `.padding(horizontal=md=12, vertical=sm=8)` | v1.7.8 | 4 个 Demo 统一补 `tour.onChange = { tour.current = $0 }`；三按钮统一加 `contentEdgeInsets=(top: sm=8, left: md=12, bottom: sm=8, right: md=12)` 对齐 Android；去掉 `nextButton`/`prevButton` 固定 `height.equalTo(36)` 让按钮按 insets 自撑开。禁令：半受控组件 Demo 必须回写组件受控属性（onChange → current），否则状态断链；按钮内边距必须与对端平台 padding 对齐，禁止裸字贴边 | :components 编译 0 错误 + iOS xcodebuild BUILD SUCCEEDED；L4 实机：Demo1 下一步可切步、Demo2 可关闭、四 Demo 按钮文字与边缘有间距（待用户实机复验） |
| 63 | ImagePreview | iOS Demo1-4 点击即 crash（Unable to activate constraint... no common ancestor） | `rebuild()` 中 `page`（新建 UIView）在 `contentStack.addArrangedSubview(page)` **之前**就执行 `page.snp.makeConstraints { make in make.width.equalTo(self) }`——page 未入视图树，与 self（ImagePreviewView）无共同祖先，AutoLayout 抛异常崩溃 | v1.7.9 | 调整为先 `contentStack.addArrangedSubview(page)` 再建 `page.width==self.width` 约束（page→contentStack→scrollView→self 共同祖先=self）；`dot` 同理先 `indicatorStack.addArrangedSubview(dot)` 再建 size 约束；Demo 4 个 preview 调用顺序调整：先 `window.addSubview+建约束` 再设 images，避免 rebuild 时 bounds=0 致 scrollTo 偏移错。禁令：跨层级约束（view.width==另一view.width）必须先 addSubview 入树再建约束，禁止未入树建涉及其他 view 的约束 | :components 编译 0 错误 + iOS xcodebuild BUILD SUCCEEDED；L4 实机：4 个 Demo 点击不再 crash、图片页可滑（待用户实机复验） |

## 第二节 早期批次（2026-08~09-04 前，按主题聚合）

| # | 组件 | 问题主题 | 典型修复提交 | 防复发落点 | 回归状态 |
|---|------|----------|--------------|------------|----------|
| 40 | Cell | iOS 行内文字垂直居中（baselineOffset 补偿反噬/移除）、横屏 contentView 不充满、失败徽标无描述 | 397ed04/4643d0f/cfe95e4 | L1（CellTest 已含 H3/H4/D5） | 部分已测（CellTest 在库） |
| 41 | Image | iOS radius 圆角不落地、demo 高度塌陷、缩放模式不对齐 | a1d6b09/0b29429/64acfce | L4+L1 | 待补测（ImageTest 在库=旧场景） |
| 42 | Button | 背景色未绑定、loading 对齐、demo addFeedbackBar 越界 | 73dbf25/5804a2a/17b4db1 | L1+L2 | 待补测 |
| 43 | Icon | SF Symbol 名不存在、非单色渲染 | fe1f7ad/d08b7c8 | L2 | 待补测 |
| 44 | Overlay | Android Dialog 拦截触摸、clip/graphicsLayer 包名错、iOS maskView/.clear/UIAction/self-before-init/闭包捕获/闭包 self/windowLevel 一批 | 85503ff→dda8278 系列 | L2（第四章钉死） | 已建成 OverlayTest 旧场景，回归批待补 |
| 45 | ConfigProvider | 无 C1.5 用户反馈错误 | — | — | ConfigProviderTest 在库 |

## 第三节 全局说明

- L1 自动化补测首批已开工组件=Switch（半受控语义）/ TextArea（maxLength/disabled/placeholder/高度数学）/ Uploader（添加格显隐/回调/disabled/结构防 crash）→ 见各组件测试文件注释映射本表 # 号。
- iOS 侧回归=恢复模拟器 XCTest（ios/Tests 已存 CellTests 等 4 件，2026-09-03 曾实跑 16/16 全绿基线）后按本表 L1 同名补测；未恢复前 iOS 行为类防复发=L2 编译门禁 + L4 实机回归清单双兜底。
- 本表 #1~#45 的 L1 项=后续 C1 单测补建任务的逐批需求（每批随组件代码一并入库并回写本表状态列）。
