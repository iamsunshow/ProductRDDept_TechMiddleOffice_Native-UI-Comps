# docs/ · 组件库文档目录（先看这里）

> 组件库规范正文一律在《产研部-唯一权威手册》，本目录不再放任何规范正文。
> **这里只有 3 篇人读文档 + 1 个数据目录**：先读本文定位，干任务看 `组件进度.md`，机器 json 一律不打开。

## 一、3 篇人读文档（唯一要读的）

| 文档 | 用途 |
|---|---|
| `README.md` | 本文=目录地图+规范入口+数据文件说明 |
| `组件进度.md` | ★接续必读=组件完善进度+分类口径（1.1 节）+双端一致性治理+平台差异登记入口+验收结论归档处 |
| `平台差异.md` | 双端系统级原生差异白名单（允许的差异只在这张表里=表外一律禁止=验收打回） |

## 二、1 个数据目录（别打开）

| 目录 | 是什么 |
|---|---|
| `数据与产物/` | 程序直读或脚本生成=**不要打开阅读**：`api.json`（组件契约）、`design-token.json`（样式）、`ui-version.json`（版本号）、`design-spec/`（规格 H5=浏览器打开验收） |

## 三、docs 之外的相关资产

| 位置 | 是什么 |
|---|---|
| `vector-store/`（仓库根） | 向量检索索引=独立存放不占文档注意力=embed.py 生成/search.py 读取 |
| `www/`（仓库根） | 组件文档站=构建时读 `docs/数据与产物/api.json` 与 `design-token.json` |

## 四、规范去哪查

验收流程/验收标准/模板/评审清单（原 `docs/验收流程/` 目录已 2026-09-04 收拢删除）→ `/Users/iamsunshow/Desktop/OPC/ProductRDDept/docs/handbook/产研部-唯一权威手册.md` 第二章；历史组件验收档案（cell/image/config-provider/overlay）结论在 `组件进度.md`，明细在 git 历史。
