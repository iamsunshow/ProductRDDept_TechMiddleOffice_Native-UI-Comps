# 多端适配说明（占位）

各端实现不强制收进 OPC；catalog 用 `source_refs` 指向业务仓或本仓 `snippets/`。

| 端 | 状态 | 备注 |
|----|------|------|
| iOS | 首批抽检自 keep-accounts | UIKit + tokens |
| Android | 首批抽检自 keep-accounts | Compose + tokens |
| Harmony | 槽位预留 | unavailable |
| 微信小程序 | 槽位预留 | unavailable |
| 支付宝小程序 | 槽位预留 | unavailable |
