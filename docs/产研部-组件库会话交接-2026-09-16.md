# 产研部 · 组件库会话交接（2026-09-16）

> 交接对象：后续接手组件库开发的 Agent
> 会话日期：2026-09-16
> 组件库版本：**v1.0.10**（CHANGELOG.md 最新条目）／ **v1.0.11**（ui-version.json）——**两者不一致，见第三章 P0-1，须先定夺**
> 子仓：ProductRDDept/TechMiddleOffice/Native-UI-Comps（git submodule，main 分支，只本地提交不 push，当前领先 origin/main **7 个提交**）
> 主仓：/Users/iamsunshow/Desktop/OPC

## 一、结论速览（先看这里）

1. **本会话是「v1.0.9 入库事故」的连环收尾修复**：v1.0.9（子仓 commit 611a17e）一次性入库 4 件组件（ShortcutBar #82 / ProgressCircle #83 / LineChart-Android #96 / BarChart #97），入库时留下 **6 类遗漏**；本会话逐轮修复，**现已双端四构建全绿**（实测，非汇报口径）：Android `:components:compileDebugKotlin` ✓ / `:app:assembleDebug` ✓ / iOS 包 `xcodebuild -scheme tmo-native-ui-comps` **BUILD SUCCEEDED** ✓ / iOS demo `ZhiqihuayunDemo` **BUILD SUCCEEDED** ✓。
2. **两处硬残留必须先修**（P0，见第三章）：① **版本号不一致**——ui-version.json = `1.0.11`，但 CHANGELOG.md 最新条目 = `[1.0.10]`、**无 [1.0.11] 条目**；② `docs/数据与产物/api.json` 顶层 `componentCount` = **90**，而 `components` 数组实际 **92** 条。
3. **本会话全部代码/文档改动仍未提交**（子仓 14 改 + 3 新增）。交接后**先提交再推进**——本文第二章即待提交内容的完整清单，第五章给建议提交拆分。

## 二、本会话已完成（编译级验证通过，改动仍待提交）

### 2.1 v1.0.9 入库事故的六轮修复

| 轮次 | 症状 | 根因 | 处置 | 落点 |
|------|------|------|------|------|
| 1 | Android 编译 `Unresolved reference 'bgGrayLight'` | v1.0.9 入库 BarChart 时双端引用 `AppColor.bgGrayLight`，但该 token 从未定义 | 双端 `AppColor` 各补 1 行（iOS `UIColor(hex:0xF3F4F6)` / Android `Color(0xFFF3F4F6)`） | `ios/Foundation/Design/AppTokens.swift`、`android/foundation/design/AppTokens.kt` |
| 2 | 用户验收反馈「只看到条形图，其他组件没看到」 | 漏注册 Demo 行 2 处 | iOS 补 `ui.shortcut-bar`（信息展示 Section）；Android 补「ProgressCircle 环形进度」（信息展示 Section） | `demo/ios/DemoApp/DemoShowcases.swift`、`demo/android/.../MainActivity.kt` |
| 3 | iOS 编译 `Cannot find 'ProgressCircleShowcase'/'ShortcutBarShowcase'/'BarChartShowcase' in scope` | 3 个 Showcase 文件漏注册 `.pbxproj`（4 个 section 全无 entry） | 各补 4 处 pbxproj 登记（UUID `BC/BD/D0/D1/D2/D3` 系） | `demo/ios/ZhiqihuayunDemo.xcodeproj/project.pbxproj` |
| 4 | iOS 编译 `Cannot find 'BarChartView'/'ProgressCircleView'/'ShortcutBarView' in scope` | demo 工程走 PBXBuildFile 直连 link、不走 SPM，库内 3 个 View 也漏登记 | 各补 4 处（UUID `BA00A1~A6` 系），并全量复查 SharedUI/Components **95 个 .swift 无遗漏** | 同上 |
| 5 | iOS 编译：3 Showcase 缺 `import UIKit/SnapKit`；`BarChartView.swift` 逃逸闭包漏 `self.` | 3 Showcase 是 DemoApp 内**唯三无 import** 的文件；行 169 同一闭包内其他 self 已显式、仅此一处漏 | 3 Showcase 补 import；`self.fillView` 补齐 | `demo/ios/DemoApp/*Showcase.swift`、`ios/SharedUI/Components/BarChartView.swift` |
| 6 | iOS 编译 `public func/var ... uses an internal type`（TrendChartView / CategoryPickerView / PeriodTabsView） | v1.0.2「public 化扫尾」只 public 了类、漏 public 化参数类型；且 **iOS 端从未定义** ChartPoint / BookkeepingCategory / ChartsPeriod | 新建 3 个 public 类型文件 + 删 DemoMocks 内 internal 兜底 + 各注册 pbxproj 4 处（共 12 处） | 见 2.3 |

### 2.2 组件视觉／布局修复

- `ios/SharedUI/Components/ShortcutBarView.swift`：
  - 抽 `ShortcutBarTokens` 常量（图标容器 26/圆角 6/图标 16/entry 内边距 6×4），消灭散落魔数。
  - 新增 `intrinsicContentSize`（自然高 = 壳 padding md×2 + entry 高），**根治宿主写死 `height=72` < 自然高 ≈81 致 entry 内容被压重叠**。
  - 重写 `rebuild()` 布局契约（对齐 Android `horizontalScroll` 1:1）：items ≤ columns → rowStack 宽 == 可视宽走 `fillEqually` 等分；items > columns → entry 定宽 `scrollView 宽/columns − 间距均摊` 撑开横滚。
- `android/sharedui/components/ShortcutBar.kt`：`clickable` 去掉自定义 `rememberRipple`，恢复默认 ripple（双端按压态一致）。
- `demo/ios/DemoApp/BarChartShowcase.swift` / `ProgressCircleShowcase.swift` / `ShortcutBarShowcase.swift`：**段容器高度链修复**——原 `make.height.equalTo(72/160/220)` 写死 + 容器无 bottom 闭合 → 容器高 0/内容溢出盖住下一段标题；改为组件走 `intrinsicContentSize` + 末元素 `bottom.equalToSuperview()` 闭合（含按钮定高 36、三档按钮定宽 76 防重叠）。

### 2.3 新建文件（iOS 业务契约类型，进 SP target）

- `ios/SharedUI/Components/ChartPoint.swift` —— `public struct ChartPoint { label, amount }`，与 Android `data class ChartPoint(label, amount)` 1:1。
- `ios/SharedUI/Components/BookkeepingCategory.swift` —— `public struct BookkeepingCategory { name, assetName, symbolName }`。
- `ios/SharedUI/Components/ChartsPeriod.swift` —— `public enum ChartsPeriod: Int, CaseIterable { week, month, year }`。

三件**均未列入** `Package.swift` 的 `exclude` → 属 `TMONativeUIComps` target，宿主 `import TMONativeUIComps` 即可获取（已核对；`Package.swift` 本次未改动）。

### 2.4 版本文件

- `docs/数据与产物/ui-version.json`：version `1.0.8` → **`1.0.11`**（releaseDate 2026-09-16、versionKey v1.0.11，changelog 字段首段新增 [1.0.11] 文案）。**注意此号与 CHANGELOG 不一致，见 P0-1。**
- `CHANGELOG.md`：新增 `## [1.0.10] - 2026-09-16` 完整条目（Fixed 四组 + Changed 一组 + 验证段）。

## 三、当前残留（下一任 Agent 按序处理）

### P0-1 版本号不一致（须先定夺）

- `docs/数据与产物/ui-version.json`：version = **`1.0.11`**（本会话由 `1.0.8` 改入，未提交）。
- `CHANGELOG.md`：最新条目为 `[1.0.9]` / `[1.0.10]` / `[1.0.8]`，**无 `[1.0.11]`**。
- 两边文案互相矛盾：ui-version.json 的 [1.0.11] 段自称「同步收口 v1.0.8→v1.0.11」；CHANGELOG [1.0.10] 段自称「v1.0.10 收口时同步升至 v1.0.10」。判定依据：CHANGELOG [1.0.10] 的正文是 ui-version.json [1.0.11] 文案的**超集**（多出 Demo 注册行补全 + ChartPoint/BookkeepingCategory/ChartsPeriod），推测是「先写 token 修复的 1.0.11 草稿 → 扩写成完整 1.0.10 条目 → 忘回改 ui-version.json」。
- **两个可选处置（本会话故意未改，属发布治理决策）**：
  - **方案 A（建议）**：以 CHANGELOG.md 为准（文档自述其为「版本日志唯一数据源」）→ ui-version.json 改回 `1.0.10`，其 changelog 首段换成 [1.0.10] 文案。
  - **方案 B**：以 ui-version.json 为准（§7h 规定版本号数据源 = ui-version.json 的 version）→ CHANGELOG.md 顶部补 `[1.0.11]` 条目。
- 决策后须连带同步：demo 徽标版本、`api.json` note 内「验证版本」字样；`Package.swift` 无版本字段（按 tag 发版，不受影响）。

### P0-2 api.json 计数不一致

- `docs/数据与产物/api.json` 顶层 `componentCount` = **90**，但 `components` 数组实际 **92** 条（CHANGELOG v1.0.9 声称「91 → 92」只落到数组、没落到计数字段）。
- 处置：把 `componentCount` 改为 92（或先确认字段语义再统一）。
- 备注：本会话四件组件（ui.shortcut-bar 行 3708 / ui.progress-circle 行 3444 / ui.line-chart 行 3617 / ui.bar-chart 行 10087）**条目均已在位**。

### P1 验收与状态核对

- 本会话**只做到编译级验证**（四构建全绿）；**C1.5 用户双端实机验收未走**。
- **须实际读注册行核对 passed 状态**：v1.0.9 汇报「3+3=6 处注册行 reviewed=true + passed=true」与「4 件」自相矛盾；本会话补了 2 处漏注册行（iOS `ui.shortcut-bar` / Android「ProgressCircle」）。四件（#82/#83/#96/#97）双端 Demo 行是否都已可达且 passed 需逐行复核。
- `docs/数据与产物/回归测试台账.md` 未为本批补行（若含可自动化项须按第四章禁令补 Robolectric 用例）。

### P2 长期遗留（贯穿多版本）

- C1 单测（本批新件是否有 Robolectric 用例待查）、C2 CR/CI、D 发版均未走。
- 子仓领先 origin/main **7 commits 未 push**（既定策略：只本地提交不 push）。
- **可信度提示**：v1.0.2 的「public 化扫尾完成」被本会话证明当时汇报不实（漏 3 个参数类型）；v1.0.9 的「BUILD SUCCEEDED」亦为漏报。**后续任何「BUILD SUCCEEDED」汇报必须真跑后回执**，不得凭推测填写。

## 四、建议提交拆分（§7d 一次提交一件事）

| 序 | 提交主题（子仓） | 含文件 |
|----|-----------------|--------|
| 1 | `fix(组件): v1.0.9 入库事故收口——bgGrayLight token + pbxproj 注册 6 处 + Demo 注册行 2 处 + 业务类型 public 化` | AppTokens 双端、pbxproj、DemoShowcases、MainActivity、DemoMocks、3 个新建件、3 个 Showcase 的 import/高度链、BarChartView self、CHANGELOG、ui-version.json |
| 2 | `fix(组件): ShortcutBar 双端视觉对齐——iOS 抽 token + intrinsicContentSize + 横滚布局契约，Android 恢复默认 ripple` | `ShortcutBarView.swift`、`ShortcutBar.kt` |
| 3 | `docs(交接): 组件库会话交接（2026-09-16）` | 本文档 |
| 4 | 主仓：更新 submodule 指针 + 按 §7g 清空 `logs/agent-ops/2026-09-16.md` |

> 说明：若执行 P0-1 方案 A/B，版本号改动随第 1 个提交一起入库（或单独一个 `chore(版本)` 提交）。

## 五、仓库状态

| 范围 | 状态 |
|------|------|
| 子仓工作区 | **14 改 + 3 新增未提交**（见第二章清单）；另有既有未跟踪产物（`ios/build/` 等，不入库不触碰） |
| 子仓分支 | main，领先 `origin/main` 7 commits（未 push） |
| 主仓工作区 | 仅 submodule 指针显示 modified（本会话子仓改动导致），另有工具生成未跟踪目录（`.workbuddy/` 等，**不碰**） |
| `logs/agent-ops/2026-09-16.md` | 当前为空模板（本次交接记录需按 §7g 追加并随主仓提交烧录） |

## 六、流程红线与环境备忘（易踩坑）

- 提交身份：`GIT_AUTHOR_NAME`/`GIT_AUTHOR_EMAIL`/`GIT_COMMITTER_NAME`/`GIT_COMMITTER_EMAIL` 标识真实提交方；标题 `type(scope): 中文一句话摘要`，正文三段式（改了什么/为什么/怎么验证），禁裸写 update；一次提交一件事；**子仓先提交 → 主仓再提交指针**。
- 文件操作后立即追加 `logs/agent-ops/当日.md`；提交时烧录进 commit 说明并**清空记录行**（保留表头）。
- 文档文件名必须含中文、章节编号必须中文；「回归台账／组件进度／api.json／ui-version.json／CHANGELOG」五件套随组件修复同步。
- Android 构建：`cd android && ./gradlew :components:compileDebugKotlin`；demo：`cd demo/android && ./gradlew :app:assembleDebug --no-daemon`；并发遇 `build.db locked` 就 `sleep 20` 重试。
- iOS 构建：包 `xcodebuild -scheme tmo-native-ui-comps -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/lib-dd build`；demo `cd demo/ios && xcodebuild -project ZhiqihuayunDemo.xcodeproj -scheme ZhiqihuayunDemo -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/demo-dd build`。**并发 xcodebuild 必须各给独立 `-derivedDataPath`**（否则撞 DerivedData 锁）。
- iOS 手工改 `.pbxproj` 后，Xcode 须**完全退出（Cmd+Q）重开**才载入新文件引用（不热载）。
- 实机汇报固定格式（§7j）：第 1 行「组件：xxx可以验证，版本：vX.Y.Z（子仓 commit xxxxxxx）」+「## 改动细节」（≤5 条），并给安装命令、Demo 位置、验收点、通过后下一步四要素。
- 本次四构建均使用的独立 derivedDataPath：`/tmp/lib-dd-0916`、`/tmp/demo-dd-0916`（可复用或另建）。

## 七、待办清单

| 优先级 | 待办 | 负责方 |
|--------|------|--------|
| 高 | 定夺并修复版本号不一致（P0-1，方案 A 或 B） | 用户拍板 / AI 执行 |
| 高 | 修 `api.json` `componentCount` 90 → 92（P0-2） | AI |
| 高 | 按第四章拆分提交本会话改动（子仓 3 提交 + 主仓指针提交） | AI |
| 高 | 请用户双端实机验收 4 件（#82/#83/#96/#97）：四段 Demo 可达、视觉与交互正常 | AI 发起 / 用户验收 |
| 中 | 逐行复核双端 Demo 注册行 passed 状态，与用户验收结论对齐 | AI |
| 中 | 回归测试台账为本批补行（可自动化项补 Robolectric，运行 `cd android && ./gradlew :components:testDebugUnitTest`） | AI |
| 低 | 组件进度.md 对应行（#82/#83/#96/#97）状态与备注同步 | AI |
| 低 | 历史遗留：C1 单测/C2 CR/CI/D 发版（长期） | AI |
