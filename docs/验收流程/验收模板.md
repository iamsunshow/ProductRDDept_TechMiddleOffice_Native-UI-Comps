# 组件验收文档 · <组件名>

> 模板说明：每个组件复制本文件为 `docs/验收流程/component-acceptance-<组件名>.md`，**五要素缺一 → 评审打回**。
> 依据：`docs/验收流程/验收标准.md` 9.6 节。流程：设计细节+设计测试用例（阶段1，门禁A）→ API细节+API测试用例（阶段2，门禁B）→ 实现并执行测试（阶段3，门禁C1）→ **Demo Showcase + 实机确认（阶段3.5，门禁C1.5）** → CR+CI（门禁C2）→ 发版 + 业务落地（门禁D）。
> 用例即代码：用例 ID（D1-D8 / A1-A7）与测试函数一一对应，命名如 `test_D1_defaultState`，CI 在门禁 C1 自动核对。

---

## 一、验收信息

| 项 | 值 |
|----|-----|
| 组件名 / id |  |
| 分类（subcategory） |  |
| 推进顺序 | 列表第 # 位 |
| 状态 | ⬜ 设计中（A）→ 📋 API（B）→ 💻 实现（C）→ ✅ |
| 验收文档 | `docs/验收流程/component-acceptance-<组件名>.md` |
| 设计规格页 | `docs/design-spec/<组件名>-design-spec.html` |

---

## 二、① 设计细节（阶段 1 · 门禁 A 评审）

> 详细内容在 H5 设计规格页，此处为索引与评审结论。

| 项 | 内容 |
|----|------|
| 组件用途 | 一句话 + 适用场景 |
| 五态定义 | 默认 / 禁用 / 加载 / 成功 / 失败（N/A 标注） |
| 尺寸规格 | 引用 design-token.json（宽高/间距/圆角/字号/色板） |
| 交互细节 | 点击反馈 / 动画 / 手势 / 层级 |
| 双端差异 | 平台差异说明（登记 平台差异.md） |
| 与现有组件关系 | deps 列表 |
| 设计参考图 | 页面链接 / 截图 |

**设计评审（门禁 A）结论：** ☐ ✅ 通过　☐ ❌ 打回　备注：

---

## 三、② 设计测试用例（阶段 1 定义 · 阶段 3 执行）

> 覆盖五态渲染、尺寸/token 取值、交互反馈。门禁 C 执行，结果随 PR 提交。

| # | 用例 | 前置 | 步骤 | 预期 | 双端 | 结果 |
|---|------|------|------|------|------|------|
| D1 | 默认态渲染 | — | 渲染默认 Cell | 标题/右侧值/图标按设计规格渲染，尺寸与 token 一致 | iOS+Android | ☐ |
| D2 | 禁用态 | — | 渲染 disabled Cell | 置灰、不可点击、不透出箭头 | iOS+Android | ☐ |
| D3 | 加载态 | — | 渲染 loading Cell | 骨架占位（shimmer） | iOS+Android | ☐ |
| D4 | 成功态 | — | 渲染 success Cell | 成功标识与配色（或 N/A） | iOS+Android | ☐ |
| D5 | 失败态 | — | 渲染 error Cell | 失败标识与配色（或 N/A） | iOS+Android | ☐ |
| D6 | Token 引用 | 代码静态检查 | 扫描实现文件 | 颜色/尺寸全部引用 token，零硬编码 | 两端 CI | ☐ |
| D7 | 按下态 | — | 按压 Cell | 按下背景 gray.4，松手恢复 | iOS+Android | ☐ |
| D8 | 分割线 | — | 相邻两行 | 1px border 分隔，无重叠 | iOS+Android | ☐ |

---

## 四、③ API 设计细节（阶段 2 · 门禁 B 评审）

> 完整契约在 `docs/api.json` + `docs/开发规则.md` 第八节，此处为评审索引。

| 能力面 | 字段 | 关键内容 |
|--------|------|----------|
| 属性 Props | `props` | 属性名 / 类型 / 默认值（双端 100% 对齐） |
| 事件 Events | `events` | 回调名 / 签名 |
| 方法 Methods | `methods` | 命令式接口（如有） |
| 能力标签 | `capabilities` | 检索标签 |
| 场景 | `scenarios` | 典型场景语料 |
| Token 依赖 | `visual_tokens` | token 名列表 |
| 组件关系 | `deps` | 依赖组件 id |
| 平台状态 | `platforms` | available / partial / unavailable |

**API 评审（门禁 B）结论：** ☐ ✅ 通过　☐ ❌ 打回　备注：

---

## 五、④ API 测试用例（阶段 2 定义 · 阶段 3 执行）

> 覆盖属性取值、事件回调、能力覆盖。门禁 C 执行，结果随 PR 提交。

| # | 用例 | 前置 | 步骤 | 预期 | 双端 | 结果 |
|---|------|------|------|------|------|------|
| A1 | 属性-默认值 | — | 不传 props 渲染 | 默认值生效（如 title 为空占位） | iOS+Android | ☐ |
| A2 | 属性-自定义 | — | 传入各 props | 渲染符合传入值，双端一致 | iOS+Android | ☐ |
| A3 | 事件-onClick | — | 点击 Cell | onClick 回调触发，参数正确（索引/数据） | iOS+Android | ☐ |
| A4 | 事件-禁用拦截 | — | 点击 disabled Cell | 回调不触发 | iOS+Android | ☐ |
| A5 | 能力-五态覆盖 | — | 切换各状态 | 与设计测试 D1-D5 对应，状态切换正确 | iOS+Android | ☐ |
| A6 | 契约-schema | api.json | 校验脚本 | props/events/methods 字段合法 | CI | ☐ |
| A7 | 契约-命名对齐 | api.json | 双端对照 | 双端 props/events 命名 100% 一致 | CI | ☐ |

---

## 六、⑤ 效果查看（Demo Showcase · 阶段 3.5 · 门禁 C1.5）

> 实现 + 自测通过后，双端 demo 添加本组件展示，用户实机查看实际效果确认。演示点：五态各一行 + 主要 props 组合 + 关键事件回调；Provider 型组件（无五态）用「配置生效对比」演示。

| 端 | 操作 | 查看内容（演示点） |
|----|------|-------------------|
| Android | Android Studio 打开 `demo/android` 运行 app（或 `./gradlew :app:assembleDebug` 装 APK），首页 → <组件名> 区 | 五态 / 配置 / 交互演示点 |
| iOS | `cd demo/ios && xcodegen generate && open *.xcodeproj`，Xcode 跑模拟器，Demo 首页 → <分类> → <组件名> | 五态 / 配置 / 交互演示点 |

**双端差异观察点**：<按 `平台差异.md` 登记项填写，如长按、触摸反馈、参数类型差异>

**实机确认（门禁 C1.5）结论：** ☐ ✅ 通过　☐ ❌ 打回　备注：

---

## 七、验收记录

| 日期 | 门禁 | 结论 | 备注 |
|------|------|------|------|
|  | A 设计评审 | 通过 / 打回 |  |
|  | B API 评审 | 通过 / 打回 |  |
|  | C1 自测对齐（单测+快照+用例映射） | 通过 / 打回 |  |
|  | C1.5 Demo Showcase + 实机确认 | 通过 / 打回 | 双端 demo 位置 / 用户实机确认 |
|  | C2 CR + CI | 通过 / 打回 |  |
|  | 发版 | 版本号 / tag |  |
|  | D 业务落地 | 接入成功 / 回退 | 接入位置 / 代码量变化，见 `docs/usage-<组件名>.md` |
