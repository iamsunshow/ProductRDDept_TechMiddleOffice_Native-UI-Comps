# TMO 组件库 · 验收流程（acceptance）

> 定位：**组件开发与验收的总入口**。做任何一个组件，从这里开始。
> 本文件夹收拢「验收流程」全部文档，与 `docs/` 下其他数据/规范文件隔离，避免混淆。

---

## 一、本文件夹文件价值

| 文件 | 作用 | 什么时候看 |
|------|------|-----------|
| `验收标准.md` | **验收标准**：五阶段 + 门禁 A/B/C1/C2/D 的完整定义（设计先行 / API 先 check / 测试门禁 / CR / 自研率） | 开始组件前必读 |
| `flow.html` | **验收流程图**（H5，浏览器打开）：依赖优先 / 暂未使用跳过 / 四阶段产物 / 门禁位置 | 对流程有疑问时 |
| `验收模板.md` | **验收文档模板**：每组件复制为 `component-acceptance-<组件名>.md`（四要素：设计细节/设计测试/API细节/API测试） | 进入阶段 1 时 |
| `评审清单.md` | **评审意见单**：设计/API 验收打勾模板，评审时复制填写留档 | 门禁 A/B 评审时 |
| `组件分色排查法.md` | **Layout 问题排查方法论**：非客户端背景同学用颜色定位视觉现象根因（红=根容器/蓝=直接容器/黄=文本列/橙=label/紫=父列表） | 现象无法一眼看出根因时 |
| `regression-lessons-cell-demo.md` | **回归教训 + 自动化门禁**：Cell Demo 双端对称性坑 + ORC 门禁用例映射 | 防回归 / 写门禁用例时 |
| `component-acceptance-<组件名>.md` | （未来产物）每组件一份验收文档 | 阶段 1-5 逐步填写 |
| `usage-<组件名>.md` | （未来产物）门禁 D 业务接入记录（替换位置/代码量变化） | 发版后 |

## 二、验收流程一页

```
读取组件列表（docs/组件进度.md，按顺序）
  → 🔗 依赖优先：deps 未完成则等待
  → ❓ 用得到？否 → ⏭【暂未使用】跳过（季度复查）
  → 阶段1 设计（①设计细节 + ②设计测试用例）──门禁 A──
  → 阶段2 API（③API 细节 + ④API 测试用例）──门禁 B──
  → 阶段3 双端实现（用例即代码：D*/A* ↔ 测试函数）
  → 门禁 C1 自测（单测 + 快照 + 用例映射）→ 阶段4 CR → 门禁 C2（CR+CI）
  → 阶段5 发版 + 业务真实接入 → 门禁 D → 验收完成 → 更新进度
```

- 完整版见 `flow.html`，细节见 `验收标准.md`。
- **用不到的组件**：在 `组件进度.md` 标 `⏭【暂未使用】` + 备注原因，直接跳过。

## 三、docs/ 根目录其他文件价值（防止混淆）

`docs/` 下除本文件夹外的文件分四类：

**① 数据源（机器/文档共同消费，改这里 = 改全局）**

| 文件 | 作用 |
|------|------|
| `api.json` | 组件 API 契约**单一数据源**（props/events/平台状态/deps），`www` 文档站与测试共用 |
| `design-token.json` | 设计 token **唯一样式数据源**（颜色/字号/圆角/间距），禁止硬编码 |
| `ui-version.json` | 全局唯一版本号（双端发版都以它为准） |

**② 规范/规则（怎么做事，已合并为 3 份）**

| 文件 | 作用 |
|------|------|
| `开发规则.md` | **开发规则**（原 rules + api-contract + schema 三合一）：命名/打包/业务库约束 + API 契约 C1-C16 + api.json 字段规范 ★沟通入口 |
| `设计规范.md` | **设计规范**（原 design-spec + token README 二合一）：Token 铁律、结构语义、双端生成 |
| `治理规范.md` | **治理规范**（原 governance + versioning + containment 三合一）：架构/收纳/版本/发版 |
| `组件分类.md` | 组件分类**唯一文档**：95 个组件分类表 + 完整度指标基准 |
| `平台差异.md` | 双端平台差异白名单（`设计规范.md` 第四节引用） |

**③ 执行进度（现在做到哪了）**

| 文件 | 作用 |
|------|------|
| `组件进度.md` | 组件完善**任务清单 + 达成情况**，验收流程按它推进 ★其他 Agent 接续必读 |

**④ 生成物/资产（不要手工改）**

| 文件 | 作用 |
|------|------|
| `组件审计.md` | 双端 API 一致性审计报告（脚本生成） |
| `design-spec/`（H5 页） | 各组件设计规格 H5 页（如 cell-design-spec.html，门禁 A 的输入资产） |
| `embeddings/`（json） | 向量检索索引数据（组件语义查询，后期功能） |

## 四、阅读路径建议

| 场景 | 先看 |
|------|------|
| 开始验收一个组件 | 本 README → `验收标准.md` → `组件进度.md` 选组件 |
| 评审设计/API | `评审清单.md` 打勾 |
| 写组件验收文档 | `验收模板.md` 复制 |
| 看整体流程 | `flow.html`（浏览器打开） |
| 想改数据/契约 | `api.json`（字段规范见 `开发规则.md` 第七节） |
| 查进度/接续工作 | `组件进度.md` |

---

## 五、🔒 L269+1 硬门禁（用户 2026-09-04 新增，凌驾于所有组件门禁之上，**所有组件永久通用**=本 README 最高优先级章节）

> 合法性唯一来源：用户 2026-09-04 原话：『如果我没有 Demo 验收你不可以启动下一个组件。』（永久有效，覆盖所有组件 SOP 所有阶段所有文档，凌驾于 `验收标准.md`/`开发规则.md`/`组件进度.md` 之上）

### 5.1 C1.5 Demo 验收=双通过=法定唯一通过标准

任何组件从「源码实现完成」走到「reviewed=True / 推进 C2 / D 发版 / 启动下一件组件 / 宣称已交付」之前，**必须同时满足下面两道，少一道=5 禁铁律全部生效，违者直接定性假交付并全链路 6 件回滚**：

✅ **通过条件 ① · 双端真 build 0 error 日志（B4-1 唯一合法）**：
- iOS = Xcode.app 直接 build / `xcodebuild clean build -project <ZhiqihuayunDemo.xcodeproj> -scheme <scheme>` 命令行；日志出现 **BUILD SUCCEEDED** 且 0 行 error（warning 不阻挡）
- Android = Android Studio 直接 assemble / `./gradlew :app:assembleDebug` 命令行；日志出现 **BUILD SUCCESSFUL** 且 0 行 error
- 🚫 **以下手段永久禁止作为 B4 通过依据**（写进 commit 也不算，违规=假交付）：`swiftc -parse`/`kotlinc -script` 等只做语法不做模块链接的快检、`awk` 括号平衡、`grep` 关键字命中、`import 存在` 等纯纸面检查

✅ **通过条件 ② · 用户亲自 Demo 运行验收签字留痕**：
- 产品负责人（用户）在设备或模拟器里亲自打开 Demo App，操作该组件 DemoShowcase 的 4 组用例（或所有注册的 Demo 组）
- 之后用户**亲口用文字或语音说**『<组件名> Demo 过了 / <组件名> Demo 验收通过 / 可以推进 C2 发版』任何一句，AI 必须把这句话原文完整抄进该组件验收文档 §8 复盘留痕=视为本条件通过
- 🚫 严禁 AI 代填或脑补「用户应该满意了所以算通过」=违者假交付

### 5.2 两道全通过前=5 禁铁律（锁死，违者自动回滚）

1. 禁 1：禁止 `api.json <component>.reviewed` False→True 切换；DemoShowcases/MainActivity 里的 reviewed=True 也禁止切（Demo 列表置灰不可点，验收标准 §9.1 L269 对齐）
2. 禁 2：禁止推进 C2 命名对齐 reviewed 冻结（命名对齐源码做完也得等，reviewed 切 True=越界）
3. 禁 3：禁止 D 发版（CHANGELOG 新段插入 / ui-version.json 升版 / 任何发版行为=一律非法，自动触发回滚）
4. 禁 4：禁止启动下一件组件（无论分类顺序下一个是谁，少一道通过=绝对不启动；WIP≤3 机制全程锁死；用户必须亲口说出「启动 <下一件组件名>」四个字才能解第 4 禁）
5. 禁 5：禁止任何形式宣称「已交付 / 已完成 / 已收官 / 已发版 / 已闭环 / C1.5 通过」=违者直接定性假交付，自动执行 6 件回滚：① reviewed=False ② ui-version 降回回滚前基线 ③ CHANGELOG 删非法段 ④ 组件进度 §3.1/§2.4/§4 全部 ✅→💻 + D ❌ ⑤ PROJECT-LIST PRJ-013 进展回减 1 ⑥ 操作日志留痕

### 5.3 落盘位置与启动 checklist

组件启动 16 勾 checklist（合规 6 项 + SOP 九阶段 = 15 勾 + L269+1 双通过 = 第 16 勾）：
- 前 15 勾详见各组件验收文档 §8.5
- 第 16 勾（本门禁）=「[ ] L269+1 双通过：真 build 0 error 日志 + 用户 Demo 验收签字留痕」=最后一道绝对锁

**下一件组件启动前的唯一合法动作**：AI 把 16 勾清单（空勾）贴给用户，说「对齐 L269+1，启动前需要您确认 16 勾可以启动打勾，勾全了我再开干」→ 用户说「勾都齐了开工吧」→ 才准推进第一步 A 设计草案。**严禁再犯"先干活后找 SOP"反序**。

### 5.4 历史治理案例（永久留痕，避免重踩）

- 2026-09-04 治理 1：Overlay 未过 A/B 用户评审就切 reviewed=True + 升版 v1.4.0 → 用户指出违规=全额回滚（5 禁铁律模板化的源头）
- 2026-09-04 治理 2：Overlay A/B 评审 21/21 合法通过后，AI 未满足 L269+1 两道门禁，就宣称 D 发版 v1.4.0 + 基础类 6/6 收官 + 已交付 → 用户 iOS Xcode 真 build 炸 3 报错实锤抓获=假交付定性=二次回滚 6 件（v1.4.0→1.3.12 + reviewed=False + CHANGELOG 删段 + 进度表回滚 + 本 5.4 治理案例永久写入=本 L269+1 硬门禁诞生的直接原因）

所有后续组件必须先读本章节，少读一步=不准开干。

